{
  "name": "smart-college-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Smart College Management System — a full-stack platform for students, faculty and college administration.",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:deploy": "npm run prisma:deploy --prefix backend",
    "prisma:studio": "npm run prisma:studio --prefix backend",
    "seed": "npm run seed --prefix backend",
    "test:backend": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
